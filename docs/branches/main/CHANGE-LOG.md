# main Change Log

## Branch Summary

- Adds a shared Copilot workspace instruction at `.github/instructions/python-standards.instructions.md` for Python authoring in `src/` and `tests/`.
- Encodes ASW-specific Python rules for future annotations, type hints, Google-style docstrings, pytest test docstrings, formatting, logging, and exception handling.
- Keeps the instruction narrowly scoped to code-authoring behavior so workflow checks and branch finalization guidance remain separate.
